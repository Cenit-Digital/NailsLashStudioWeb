{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "_comment": "Prueba de mutación (RF-CODE-001 / GU-HARNESS-001). Valida que los tests muerden. Umbral: 100% sobre las líneas tocadas por la feature. `mutate` es una lista EXPLÍCITA de ficheros (no un glob): cada feature añade los suyos (src/lib/*.ts y src/components/*.tsx). Quedan fuera por convención del stack: App.tsx, main.tsx, pages/* y styles/*. PROHIBIDO acotar con --testFiles (da 0% falso con este stack); acotar solo con --mutate <fichero>. `ignorePatterns` excluye .experimentos-tmp: los builds SSG de @s32/@s33 (src/lib/trampas-del-horneado.test.tsx) escriben ahi, y si un resto queda del run anterior Stryker lo copia a su sandbox y ENVENENA la tanda (dry run parcial, cobertura rota, score de 0,21% que no significa nada). Paso por el que ya se perdio una medicion.",
  "packageManager": "pnpm",
  "testRunner": "vitest",
  "plugins": [
    "@stryker-mutator/vitest-runner"
  ],
  "reporters": [
    "html",
    "clear-text",
    "progress"
  ],
  "coverageAnalysis": "perTest",
  "vitest": {
    "configFile": "vitest.config.ts"
  },
  "mutate": [
    "src/lib/placeholders.ts",
    "src/lib/puerta.ts",
    "src/lib/site.ts",
    "src/lib/contraste.ts",
    "src/lib/puerta-contraste.ts",
    "src/lib/seo.ts",
    "src/lib/puerta-cascaron.ts"
  ],
  "thresholds": {
    "high": 100,
    "low": 90,
    "break": 100
  },
  "tempDirName": ".stryker-tmp",
  "htmlReporter": {
    "fileName": "reports/mutation/index.html"
  },
  "ignorePatterns": [
    ".experimentos-tmp",
    "dist",
    "reports",
    "coverage"
  ]
}

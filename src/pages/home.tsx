// Encabezado PROVISIONAL de andamiaje: solo existe para que las puertas del arnés
// (test, build/prerender) tengan algo que ejercitar. El contenido real de la home
// llega por features con TDD; no añadas aquí datos del salón.
export default function Home() {
  return <h1>Nails & Lash Studio</h1>
}

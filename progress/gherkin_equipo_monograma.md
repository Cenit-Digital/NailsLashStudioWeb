# Ampliación del contrato de equipo — el monograma

`features/equipo_reservas.feature` pasa de 25 a 31 escenarios: @s26 (las 7 tarjetas muestran la inicial real de `equipo-demo.ts`: L C A N M P S) · @s27 (la inicial es SIEMPRE mayúscula, función pura, incluida la acentuada «ángela»→«Á») · @s28 (caso límite: nombre vacío → cadena vacía, sin guarda redundante ni excepción) · @s29 (monograma DECORATIVO: `aria-hidden`, ninguna letra suelta en el árbol de accesibilidad) · @s30 (las 7 iniciales viajan HORNEADAS en los bytes de `dist/`) · @s31 (sigue sin fotos: cero `ph-woman`, cero `<img>`, cero terceros; el par `--accent-dark`/`--accent-soft` ya está en la matriz, `MINIMO_DE_PARES` sigue en 18). Sin tocar `src/`, tests ni `feature_list.json` (§18 `equipo` sigue `blocked`; D7 exige desbloqueo humano explícito). Nueva decisión a la puerta humana: **D8**, el aspecto del monograma.
